services:
  - runtime: docker
    type: web
    name: example_tracing_api
    dockerfilePath: ./Dockerfile
    plan: free

  - runtime: docker
    type: web
    name: gr_alloy
    dockerfilePath: ./alloy/Dockerfile
    plan: free
